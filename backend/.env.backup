
# DATABASE_URL is set in .env.local with absolute path for npx prisma compatibility
# For application code, use the relative path below (resolved by config.ts)

# DATABASE_URL is set in .env.local with absolute path for npx prisma compatibility
# For application code, use the relative path below (resolved by config.ts)
DATABASE_URL=file:./prisma/dev.db
