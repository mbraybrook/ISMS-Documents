LOAD DATABASE
  FROM sqlite:///home/developer/dev/ISMS-Documentation/backend/prisma/dev.db
  INTO postgresql://postgres:postgres@localhost:5433/isms_db

WITH create tables, create indexes, reset sequences

SET work_mem to '256MB',
    maintenance_work_mem to '512MB'

CAST type datetime to timestamptz,
     type date to date

BEFORE LOAD DO
  $$ ALTER DATABASE isms_db SET timezone TO 'UTC'; $$;

